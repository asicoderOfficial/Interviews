#include <iostream>
#include <vector>
#include <utility>

using namespace std;

/**
 * 1 -> Pick the smallest edge.
 * 2 -> Repeatedly look for the smallest edges that
 *      don't create a cycle.
 */

/**
 * Limited by the process of edges sorting.
 * Time complexity: O(E logE), E = number of edges, with merge sort.
 */


int main() {

return 0;
}